# deploy/README.md says to `scp -r .` to the server, so the working copy must already be
# LF — a CRLF shebang fails on Linux with "bad interpreter: /bin/bash^M".
* text=auto
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
