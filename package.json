{
  "name": "nginx-dashboard",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Web dashboard that controls NGINX via buttons instead of the command line",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "demo": "node scripts/demo.mjs",
    "totp:new": "node scripts/totp.mjs",
    "test": "node test/docs.mjs && node test/smoke.mjs && node test/safe-apply.mjs && node test/client.mjs",
    "test:floor": "node test/version-floor.mjs",
    "test:docker": "node test/docker-conf.mjs",
    "test:e2e": "docker build -f test/Dockerfile.e2e -t nginx-dashboard-e2e . && docker run --rm nginx-dashboard-e2e"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "chart.js": "^4.4.0",
    "qrcode-generator": "^2.0.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
